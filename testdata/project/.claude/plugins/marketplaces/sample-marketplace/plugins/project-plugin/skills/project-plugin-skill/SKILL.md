---
name: project-plugin-skill
description: Skill installed at project scope via a marketplace plugin
---

# Project Plugin Skill

Lives at `<repo>/.claude/plugins/marketplaces/<mkt>/plugins/<plugin>/skills/<skill>/SKILL.md` —
the result of `/plugin install <pkg> --scope project`.
